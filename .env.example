# ----------------------------------------------------------------------------
# GreenSteps – Environment Variables
# Copy to `.env` and fill in real values. Never commit `.env` to source control.
# ----------------------------------------------------------------------------

# --- Database (Neon Postgres with pgvector extension enabled) ---
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
DIRECT_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# --- Clerk Authentication ---
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_..."
CLERK_SECRET_KEY="sk_test_..."
NEXT_PUBLIC_CLERK_SIGN_IN_URL="/sign-in"
NEXT_PUBLIC_CLERK_SIGN_UP_URL="/sign-up"
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL="/dashboard"
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL="/dashboard"

# --- Google Gemini AI ---
# Either GEMINI_API_KEY or GOOGLE_AI_API_KEY works.
# Either GEMINI_MODEL or GEMINI_CHAT_MODEL works.
GEMINI_API_KEY="your-gemini-api-key"
GEMINI_MODEL="gemini-1.5-flash"
GEMINI_EMBEDDING_MODEL="text-embedding-004"

# --- Admin allowlist ---
# Grant the ADMIN role by Clerk user ID and/or by email (comma-separated).
# Either list matching the signed-in user promotes them on next request.
# Email is usually easier — you know yours without opening the Clerk dashboard.
ADMIN_USER_IDS=""
ADMIN_EMAILS="you@example.com"

# --- App ---
NEXT_PUBLIC_APP_URL="http://localhost:3000"
